<Window x:Class="LocationInterface.Windows.PresetNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Width="300"
        Height="75"
        Title="Enter Preset Name" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="valueEntry" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" TextWrapping="Wrap" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Button Grid.Column="0" Grid.Row="1" Content="Submit" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="SubmitButtonClick"/>
        <Button Grid.Column="1" Grid.Row="1" Content="Cancel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="CancelButtonClick"/>
    </Grid>
</Window>
